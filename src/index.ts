/**
 * high quality scream string
 * @returns {"waaaaa"}
 */
export function waaaa() {
  return "waaaaa";
}

/**
 * nice function for saying heyy
 * @returns {"heyy"}
 */
export function heyy() {
  return "heyy";
}

/**
 * rather nicely adds 2 numbers rather nicely
 * @param a
 * @param b
 * @returns {number}
 */
export function adder(a: number, b: number): number {
  return a + b;
}
